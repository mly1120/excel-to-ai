{
  "name": "@excel-to-ai/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --out-dir dist --clean",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/app.test.ts src/lib/province.test.ts src/modules/ai/provider.test.ts src/modules/executor.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/multipart": "^9.2.1",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
